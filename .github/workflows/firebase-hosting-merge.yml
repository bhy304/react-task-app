# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  push:
    branches:
      - main
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '20'
      - run: npm install
      - name: Check environment variables
        run: |
          echo "Checking Firebase environment variables..."
          if [ -z "$VITE_API_KEY" ]; then echo "ERROR: VITE_API_KEY not set"; exit 1; fi
          if [ -z "$VITE_AUTH_DOMAIN" ]; then echo "ERROR: VITE_AUTH_DOMAIN not set"; exit 1; fi
          if [ -z "$VITE_PROJECT_ID" ]; then echo "ERROR: VITE_PROJECT_ID not set"; exit 1; fi
          if [ -z "$VITE_STORAGE_BUCKET" ]; then echo "ERROR: VITE_STORAGE_BUCKET not set"; exit 1; fi
          if [ -z "$VITE_MESSAGING_SENDER_ID" ]; then echo "ERROR: VITE_MESSAGING_SENDER_ID not set"; exit 1; fi
          if [ -z "$VITE_APP_ID" ]; then echo "ERROR: VITE_APP_ID not set"; exit 1; fi
          echo "✓ All Firebase environment variables are set"
        env:
          VITE_API_KEY: ${{ secrets.VITE_API_KEY }}
          VITE_AUTH_DOMAIN: ${{ secrets.VITE_AUTH_DOMAIN }}
          VITE_PROJECT_ID: ${{ secrets.VITE_PROJECT_ID }}
          VITE_STORAGE_BUCKET: ${{ secrets.VITE_STORAGE_BUCKET }}
          VITE_MESSAGING_SENDER_ID: ${{ secrets.VITE_MESSAGING_SENDER_ID }}
          VITE_APP_ID: ${{ secrets.VITE_APP_ID }}
      - name: Build with environment variables
        run: npm run build
        env:
          VITE_API_KEY: ${{ secrets.VITE_API_KEY }}
          VITE_AUTH_DOMAIN: ${{ secrets.VITE_AUTH_DOMAIN }}
          VITE_PROJECT_ID: ${{ secrets.VITE_PROJECT_ID }}
          VITE_STORAGE_BUCKET: ${{ secrets.VITE_STORAGE_BUCKET }}
          VITE_MESSAGING_SENDER_ID: ${{ secrets.VITE_MESSAGING_SENDER_ID }}
          VITE_APP_ID: ${{ secrets.VITE_APP_ID }}
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_REACT_TASK_APP_90283 }}
          channelId: live
          projectId: react-task-app-90283
